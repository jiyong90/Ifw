<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.isu.ifw.mapper.WtmOtCanApplMapper"> 
	 <select id="otCanApplfindByApplId" parameterType="Long" resultType="hashmap">
        <![CDATA[
			SELECT O.OT_CAN_APPL_ID AS otCanApplId
			     , O.APPL_ID AS applId
			     , O.OT_APPL_ID AS otApplId
			     , O.WORK_DAY_RESULT_ID AS workDayResultId
			     , O.YMD AS ymd
			     , O.TIME_TYPE_CD AS timeTypeCd
			     , O.PLAN_SDATE AS planSdate
			     , O.PLAN_EDATE AS planEdate
			     , O.PLAN_MINUTE AS planMinute
			     , O.APPR_SDATE AS apprSdate
			     , O.APPR_EDATE AS apprEdate
			     , O.APPR_MINUTE AS apprMinute
			     , O.REASON AS reason 
			     , A.APPL_STATUS_CD AS applStatusCd
			  FROM WTM_APPL A
			  JOIN WTM_OT_CAN_APPL O
			    ON A.APPL_ID = O.APPL_ID
			 WHERE O.APPL_ID = #{applId}    
	        ]]>
    </select>
     
   
</mapper>
